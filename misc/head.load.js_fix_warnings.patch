--- public/js/head.load.js
+++ public/js/head.load.js
@@ -180,7 +180,7 @@
         items = items || assets;
 
         for (var name in items) {
-            if (items.hasOwnProperty(name) && items[name].state !== LOADED) {
+            if (items.hasOwnProperty(name) && items[name] && items[name].state !== LOADED) {
                 return false;
             }
         }
